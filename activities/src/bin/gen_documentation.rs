/// A favorite color
enum Color {
    Red,
    Blue,
}

/// A piece of mail
struct Mail {
    address: String,
}

/// Adds tow numbers together
fn add(a: i32, b: i32) -> i32 {
    a + b
}

/// Generate docs using: cargo doc --open
fn main() {}
